<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Client"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
		x:Class="Client.MainWindow"
        Title="MainWindow" 
		mc:Ignorable="d" 
		Height="350" Width="525" Loaded="Window_Loaded">
    <Grid Margin="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
            <Label>Type de vue</Label>
            <ComboBox Margin="0,0,3,0" Name="ViewType" SelectionChanged="ViewType_SelectionChanged">
                <ComboBoxItem>Université</ComboBoxItem>
                <ComboBoxItem>Campus</ComboBoxItem>
                <ComboBoxItem>Période</ComboBoxItem>
                <ComboBoxItem>Classe</ComboBoxItem>
                <ComboBoxItem>Utilisateur</ComboBoxItem>
            </ComboBox>

            <ComboBox Margin="0,0,3,0" Visibility="Collapsed" Name="CampusName" ItemsSource="{Binding}" SelectedIndex="0" SelectionChanged="CampusName_SelectionChanged" />
            <ComboBox Margin="0,0,3,0" Visibility="Collapsed" Name="PeriodName" ItemsSource="{Binding}" SelectedIndex="0" SelectionChanged="PeriodName_SelectionChanged" />
            <ComboBox Margin="0" Visibility="Collapsed" Name="ClassName" ItemsSource="{Binding}" SelectedIndex="0" />
        </StackPanel>

        <TabControl SelectedIndex="2" Grid.Column="0" Grid.Row="1" Margin="0,3">
            <TabItem>
                <TabItem.Header>
                    <TextBlock>Semaine</TextBlock>
                </TabItem.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
            
                    <Grid.RowDefinitions>
                        <RowDefinition />
                    </Grid.RowDefinitions>
            
                    <TextBlock Grid.Column="0" Grid.Row="0">Lundi</TextBlock>
                    <TextBlock Grid.Column="1" Grid.Row="0">Mardi</TextBlock>
                    <TextBlock Grid.Column="2" Grid.Row="0">Mercredi</TextBlock>
                    <TextBlock Grid.Column="3" Grid.Row="0">Jeudi</TextBlock>
                    <TextBlock Grid.Column="4" Grid.Row="0">Vendredi</TextBlock>
                    <TextBlock Grid.Column="5" Grid.Row="0">Samedi</TextBlock>
                    <TextBlock Grid.Column="6" Grid.Row="0">Dimanche</TextBlock>
                </Grid>
            </TabItem>
            
            <TabItem>
                <TabItem.Header>
                    <TextBlock>Jour</TextBlock>
                </TabItem.Header>
                
                <Grid Name="CalendarDay">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Column="1" Grid.Row="0">Lundi</TextBlock>

                    <ScrollViewer Margin="0" Grid.Column="1" Grid.Row="1">
                        <local:CalendarDay />
                    </ScrollViewer>
                </Grid>
            </TabItem>
            
            <TabItem>
                <TabItem.Header>
                    <TextBlock>Liste</TextBlock>
                </TabItem.Header>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <Label>Début</Label>
                        <DatePicker Name="StartDate"/>
                        <Label>Fin</Label>
                        <DatePicker Name="EndDate"/>
                    </StackPanel>
                    
                    <DataGrid Grid.Row="1" Name="EventsGrid" AutoGenerateColumns="False" ItemsSource="{Binding}"
                              CanUserAddRows="True" CanUserDeleteRows="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type" Binding="{Binding LinkedTo}"/>
                            <DataGridTextColumn Header="Desctiption" Binding="{Binding Name}"/>
                            <DataGridTextColumn Header="Salle" Binding="{Binding Place}"/>
                            <DataGridTextColumn Header="Intervenant" Binding="{Binding Speaker}"/>
                            <DataGridTextColumn Header="Matière" Binding="{Binding Subject}"/>
                            <DataGridTemplateColumn Header="Début">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Start}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <DatePicker SelectedDate="{Binding Start}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Fin">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding End}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <DatePicker SelectedDate="{Binding End}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridCheckBoxColumn Header="Obligatoire" Binding="{Binding Mandatory}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

		<Button Content="clic la" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" Name="truc" VerticalAlignment="Top" Width="75" Click="button1_Click_1" />
		<Button Grid.Column="0" Grid.Row="2" Content="TestApi" HorizontalAlignment="Right" Name="button1" VerticalAlignment="Top" Width="75" Click="button1_Click" />
    </Grid>
</Window>
